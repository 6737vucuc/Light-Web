# دليل ميزات الأمان المتقدمة (Advanced Security Features)

تم تعزيز النظام المدمج بمجموعة من الأدوات التي تمنح المستخدم تحكماً كاملاً في أمان حسابه.

---

## 🛠️ الميزات الجديدة المضافة

### 1. لوحة تحكم الأجهزة الموثوقة (Trusted Devices Dashboard)
- **المسار:** `/settings/security`
- **الوظيفة:** يمكن للمستخدم رؤية جميع الأجهزة التي تم توثيقها مسبقاً مع تفاصيل مثل (نوع الجهاز، المتصفح، الموقع الجغرافي، وآخر وقت للاستخدام).
- **الإدارة:** يمكن حذف (Revoke) أي جهاز لم يعد موثوقاً، مما سيجبر ذلك الجهاز على طلب رمز تحقق 2FA في المرة القادمة.

### 2. سجل نشاط الأمان (Security Activity Log)
- **الوظيفة:** تتبع حي لجميع العمليات الحساسة على الحساب.
- **الأحداث المسجلة:**
  - تسجيل الدخول الناجح والفاشل.
  - توثيق الأجهزة الجديدة.
  - التحقق من رموز 2FA.
  - تسجيل الخروج العالمي.
  - إغلاق الحساب بسبب محاولات فاشلة.

### 3. تسجيل الخروج من جميع الأجهزة (Global Logout)
- **الميزة:** في حال الشك في اختراق الحساب، يمكن للمستخدم بضغطة زر واحدة:
  - تسجيل الخروج من جميع الجلسات الحالية.
  - حذف جميع الأجهزة الموثوقة (بحيث يطلب النظام كود 2FA من الجميع).
  - مسح جميع البيانات المخزنة محلياً (Cookies & Storage).

---

## 🔧 التغييرات التقنية

- **قاعدة البيانات:** إضافة جدول `security_logs` وتطوير جدول `trusted_devices`.
- **API:** 
  - `GET /api/auth/security`: لاسترجاع البيانات.
  - `DELETE /api/auth/security`: لحذف جهاز معين.
  - `POST /api/auth/logout-all`: لتنفيذ تسجيل الخروج الشامل.
- **الواجهة الأمامية:** إضافة صفحة كاملة لإدارة الأمان في الإعدادات.

---

## 🔒 كيف تستخدم هذه الميزات؟

1. اذهب إلى قائمة **Security** من الشريط العلوي.
2. راجع قائمة الأجهزة الموثوقة، وإذا وجدت جهازاً غريباً، قم بحذفه فوراً.
3. راجع سجل النشاط للتأكد من عدم وجود محاولات دخول فاشلة من مواقع غريبة.
4. في حالات الطوارئ، استخدم زر **Sign Out from All Devices** في أسفل الصفحة.

**✨ حسابك الآن محمي بأحدث معايير الأمان المدمجة!**
